CREATE TABLE irmantasramanauskas (
    id int(5) not null,
    vardas varchar(20),
    epastas varchar(50),
    kam varchar(20),
    data datetime,
    ip varchar(20),
    zinute text,
	PRIMARY KEY (id)
);